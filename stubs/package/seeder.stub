<?php

namespace {{ namespace }};

use Illuminate\Database\Seeder;

class PackageSeeder extends Seeder
{
    public function run(): void
    {
        $this->call(array_map(fn ($s) => __NAMESPACE__ . "\\$s", config('{{ identity }}{{ registrar }}.seeders') ?? []));
    }
}
