@extends("{{ extend }}")

@section("app-content")

@yield("page-content")
{{ lw_scripts }}
@endsection

@push("app-scripts")
    <script src="{{ asset('js/{{ app }}.js') }}" defer></script>
@endpush

@push("app-styles")
    <link rel="stylesheet" href="{{ asset('css/{{ app }}.css') }}">
    {{ lw_styles }}
@endpush